@component-name = "portal-lima"
definition {

	property testray.main.component.name = "Blogs";

	setUp {
		task ("Given the Blogs Portlet is added to the page") {
			TestCase.setUpPortalInstance();

			User.firstLoginPG();

			JSONLayout.addPublicLayout(
				groupName = "Guest",
				layoutName = "Blogs Page");

			JSONLayout.addWidgetToPublicLayout(
				column = 1,
				groupName = "Guest",
				layoutName = "Blogs Page",
				widgetName = "Blogs");

			Navigator.gotoPage(pageName = "Blogs Page");
		}
	}

	tearDown {
		var testLiferayVirtualInstance = PropsUtil.get("test.liferay.virtual.instance");

		if (${testLiferayVirtualInstance} == "true") {
			PortalInstances.tearDownCP();
		}
		else {
			BlogsEntry.tearDownCP();
		}
	}

	@description = "Can Delete a Blog"
	@priority = 5
	test CanDeleteBlog {
		task ("And two Blog Entries exists") {
			JSONBlog.addEntry(
				entryContent = "Blog Henrique Content 1",
				entryTitle = "Blog Henrique Title 1");

			JSONBlog.addEntry(
				entryContent = "Blog Henrique Content 2",
				entryTitle = "Blog Henrique Title 2");

			Navigator.gotoPage(pageName = "Blogs Page");

			BlogsEntry.viewEntryPG(
				entryContent = "Blog Henrique Content 1",
				entryTitle = "Blog Henrique Title 1");

			BlogsEntry.viewEntryPG(
				entryContent = "Blog Henrique Content 2",
				entryTitle = "Blog Henrique Title 2");
		}

		task ("When the User deletes Blog Entry 1") {
			BlogsEntry.moveToRecycleBinPG(
				entryContent = "Blog Henrique Content 1",
				entryTitle = "Blog Henrique Title 1");
		}

		task ("Then Blog Entry 1 should not exist") {
			BlogsEntry.viewNoPG(entryTitle = "Blog Henrique Title 1");
		}

		task ("And Blog Entry 2 should exist") {
			BlogsEntry.viewEntryPG(
				entryContent = "Blog Henrique Content 2",
				entryTitle = "Blog Henrique Title 2");
		}
	}

}